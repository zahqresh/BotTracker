'use strict';

var _mobileDetect = require('mobile-detect');

var _mobileDetect2 = _interopRequireDefault(_mobileDetect);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = function (_ref) {
  var headers = _ref.headers;
  var userAgent = headers['user-agent'];
  var isMobileViewer = headers['cloudfront-is-mobile-viewer'];
  var isTabletViewer = headers['cloudfront-is-tablet-viewer'];

  var md = new _mobileDetect2.default(userAgent);

  var tablet = isTabletViewer === 'true' || !!md.tablet();
  var mobile = isMobileViewer === 'true' || !!md.mobile();
  var desktop = !mobile && !tablet;

  return {
    desktop: desktop, mobile: mobile, tablet: tablet
  };
};